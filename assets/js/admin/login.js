import {togglePassword, toggleTheme, animateAndRemoveAlert} from "../index.js";

document.addEventListener("DOMContentLoaded", () => {
    togglePassword();
    //toggleTheme();
    //animateAndRemoveAlert();
});